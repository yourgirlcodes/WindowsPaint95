(function () {
    var checkInput;
    var flag = fasle;
    checkInput.addEventListener('change', function () {
        if (!flag) {
            // mouse over is active
        } else {
            // stamp is active, mouseover is inactive
        }
    });
})();